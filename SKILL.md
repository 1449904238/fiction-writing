---
name: "小说全流程创作"
description: "小说创作全流程编排技能。从核心灵感到正文终稿，分7步推进：项目初始化→设定架构→大纲→细纲→扩写→精修→去AI味，支持从任意步骤开始，每步完成后确认再进入下一步。扩展技能包括拆文(含扫榜调研+对标节奏迁移M1-M5)/短篇/导入/审稿。当用户说我写小说、创作网文、需要全流程辅助时调用。"
version: "6.0.0"
references:
  # 快速入门（V6.0新增）
  - "QUICKSTART.md"                      # V6.0: 3步快速上手指南
  # 核心流水线 (0-6步)
  - "00.5_项目初始化.md"
  - "00_小说设定架构师.md"
  - "01_小说大纲构建师.md"
  # V5.2: 02拆分为02a(细纲编写)+02b(细纲质检)
  - "02a_细纲生成.md"
  - "02b_细纲质检.md"
  # V5.1: 03拆分为03a(扩写)+03b(质检)
  - "03a_扩写执行.md"
  - "03b_质量自检.md"
  # V5.4: 04/05模块化拆分——核心层(活跃入口)+扩展模块(按需加载)
  - "04_core.md"
  - "04b_深度打磨.md"
  - "05_core.md"
  # 扩展技能
  - "06_小说拆文师.md"
  - "07_小说导入师.md"
  - "08_短篇写作技能.md"
  - "09_多视角审稿师.md"
  - "10_读者反馈注入师.md"
  # 场景触发加载路由
  - "references/scene-trigger-map.md"
  # V6.0: 从SKILL.md拆分外置的策略文档
  - "references/上下文预算管理器.md"       # V6.0: 上下文三层预算管理
  - "references/逆向调整机制.md"           # V6.0: 正文偏差→逆向调整六步法
  - "references/批量模式.md"               # V6.0: 半自动批量执行模式
  - "references/AI使用策略.md"             # V6.0: AI能力边界与对冲设计
  # V5.1: references合并综合文件
  - "references/03_检测器_综合.md"
  - "references/04_精修协议综合.md"
  - "references/语言素材综合库.md"
  - "references/方法论综合.md"
  - "references/协议综合.md"
  # V5.4: 04/05模块化拆分扩展模块
  - "references/04_质感深化.md"
  - "references/04_技法_感官入口.md"
  - "references/04_技法_潜台词.md"
  - "references/04_标点收尾.md"
  - "references/05_可选Gate详细.md"
  - "references/05_三遍法与白名单.md"
  - "references/05_改写范例与素材.md"
  # 03质感系统（独立保留）
  - "references/03_质感注入协议.md"
  - "references/03_情绪工程协议.md"
  # 精简文笔协议（独立保留）
  - "references/05_精简文笔协议.md"
  # 平台规则配置（V6.0: 9平台通用配置）
  - "references/平台规则配置.md"
  # V6.0: 角色弧光模块（02a按需加载）
  - "references/Part_ARC_角色弧光.md"
  # V6.0: 复杂度冻结协议
  - "references/复杂度冻结协议.md"
  # 核心素材库（独立保留）
  - "references/打斗动作库.md"
  - "references/改写范例库.md"
  - "references/感官细节库.md"
  - "references/人物声口对话风格库.md"
  - "references/场景氛围环境渲染库.md"
  - "references/情绪颗粒度心理描写库.md"
  # 确定性脚本
  - "scripts/check-ai-patterns.js"
  - "scripts/check-degeneration.js"
  - "scripts/normalize-punctuation.js"
  - "scripts/check-rhythm.js"
  - "scripts/check-consistency.js"
  - "scripts/extract-used-patterns.js"
  - "scripts/compress-handoff.js"
  - "scripts/sync-versions.js"
  - "scripts/check-emotion-density.js"
  - "scripts/merge-delta.js"
  # V6.0: 新增确定性脚本
  - "scripts/check-rule-execution.js"     # V6.0: 规则执行遥测
  - "scripts/generate-dashboard.js"       # V6.0: HTML进度面板生成
  - "scripts/estimate-context-load.js"    # V6.0: 上下文预算估算
  - "scripts/build-index.js"              # V6.0: 轻量文件索引
  # 产出样本验证体系
  - "samples/README.md"
  - "samples/config/rules.json"           # V6.0: 规则开关+分级+版本化
  # 对抗性审查Agent
  - ".claude/agents/quality-checker.md"
---
# 小说全流程创作 V6.0

> **V6.0 更新**（2026-07-18）：平台通用化+规则分级+SKILL.md瘦身+执行遥测。
> **P0级（架构瘦身）**：①SKILL.md从660行瘦身至≤400行——上下文预算管理器/逆向调整机制/批量模式/AI使用策略4大段拆分外置至references/；②新建QUICKSTART.md 3步快速上手指南。
> **P1级（平台通用化）**：③平台规则配置从4平台扩展至9平台（番茄/七猫/起点/晋江/B站轻小说/飞卢/纵横/知乎盐言/通用），新增结构化YAML配置；④00.5项目初始化平台列表同步更新。
> **P2级（规则工程化）**：⑤rules.json升级至V6.0——新增severity字段（hard/soft/adaptive三档分级）+rule_version/changelog字段（per-rule版本化）+rule_lock支持（项目级规则锁定）；⑥新增check-rule-execution.js规则执行遥测脚本；⑦新增generate-dashboard.js HTML进度面板；⑧新增estimate-context-load.js上下文预算估算+build-index.js轻量文件索引；⑨新建Part_ARC_角色弧光模块（02a按需加载）。
> 
> **V5.4 更新**（2026-07-17）：Delta状态更新+state-degraded降级+力量体系深化+04/05模块化拆分。详见 [CHANGELOG.md](CHANGELOG.md)。
> 
> 完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。

## 跨平台兼容（v2.0 新增）

本 skill 支持以下四个 IDE 平台，目录结构按平台约定部署：

| 平台 | 配置目录 | Skill 安装位置 | Hook 格式 |
|------|---------|--------------|-----------|
| **OpenCode**（默认） | `.opencode/` | `~/.config/opencode/skills/` | `.ps1` / `.sh` |
| **Claude Code** | `.claude/` | `.claude/skills/` | `.sh`（bash） |
| **Codex** | `.codex/` | `.codex/skills/` | `.sh`（bash） |
| **Trae** | `.trae/` | `.trae/skills/` | `.ps1` / `.sh` |

**跨平台规则**：
1. 所有 skill 文件（.md）是平台无关的纯 prompt，任何 IDE 都可直接加载
2. hooks 按平台格式提供：Windows 用 `.ps1`，Linux/Mac 用 `.sh`
3. Agent 配置文件按平台约定放在对应目录下，不可用则降级为主线程执行
4. settings 文件不跨平台覆盖——部署时只写当前平台的配置，保留用户已有配置

## 发布平台通用化（V6.0 新增）

本 skill 支持 **9个发布平台**，通过 `references/平台规则配置.md` 进行平台适配：

| 平台 | 类型 | 默认字数/章 | 特色 |
|------|------|-----------|------|
| 番茄小说 | 免费 | 3000-4000 | 算法推荐/爽文导向 |
| 七猫小说 | 免费 | 3000-4000 | 下沉市场/短章节 |
| 起点中文网 | 付费 | 3000-5000 | 精品路线/VIP订阅 |
| 晋江文学城 | 付费 | 3500-6000 | 女频/情感细腻 |
| B站轻小说 | 付费 | 2500-4000 | 二次元/轻小说风 |
| 飞卢小说 | 付费 | 3000-5000 | 同人/快节奏 |
| 纵横中文网 | 付费 | 3000-4500 | 传统网文 |
| 知乎盐言 | 付费 | 短篇为主 | 高质量短篇 |
| 通用网文 | — | 3500-4500 | 默认配置 |

> **使用方式**：在 `00.5_项目初始化.md` 中选择目标平台，后续所有步骤自动读取 `平台规则配置.md` 中对应平台的参数（字数/章数/钩子强度/对话比/更新节奏等）。平台规则配置文件同时提供结构化YAML配置，供脚本直接读取。

## 上下文预算管理器（V5.4新增 — 借鉴InkOS protected/compressible分层）

> 详见 [references/上下文预算管理器.md](references/上下文预算管理器.md)。
> **核心**：protected（不可压缩）/ compressible（可压缩）/ droppable（可丢弃）三层优先级，超限时自动丢弃低优先内容。
> **预算估算**：运行 `node scripts/estimate-context-load.js --scene=<场景类型>` 估算当前场景需要加载的文件总行数，超2000行时告警。
> **数值锚点不可压缩**：`number_anchors` 和 `power_system` 字段在任何压缩操作中必须完整保留。

---

## 概述

此技能是小说创作的总控流程，串联**7步核心流水线 + 4个扩展技能 + 3个确定性脚本**，按固定顺序逐阶段推进。每个阶段完成后必须等待用户确认，不可自行跨越。

## 7步流水线

| 步骤 | 子技能 | 执行频率 | 产物 |
|------|--------|:---:|------|
| 第0步 | `项目初始化师` | 全书1次 | 标准目录结构 + 上下文管理模板 |
| 第1步 | `小说设定架构师` | 全书1次 | 世界观、人物设定、成长体系、关系动力学等结构化设定文档 |
| 第2步 | `小说大纲构建师` | 全书1次 | 完整情节大纲，符合签约标准 |
| 第3步 | `小说细纲编写技能` | 滚动建纲 | 章节级细纲（五段式+字数预算+大纲五检），先建前10章，每写3章补5-10章 |
| 第4a步 | `细纲扩写执行` (03a) | **逐章循环** | 小说正文初稿（生成+质感注入+情绪工程） |
| 第4b步 | `质量自检` (03b) | **逐章循环** | 质检报告（P1抽查+P2可选+5维评分+快速通道判断） |
| 第5步 | `小说正文精修师` (04) / `深度打磨` (04b) | **逐章循环** | 精修后的正文（潜台词、金句、风格） |
| 第6步 | `去AI味精修师` (05) | **逐章循环** | 最终定稿（11 Gate + 工程化保护 + 确定性脚本兜底） |

> 第0-3步 = 一次性准备（第3步支持滚动建纲，全书≤30章可一次全建） | 第4a-6步 = 逐章循环（每章重复）
> V5.2: 第4b步（03b质检）为**每章必执行**，在独立上下文中运行，质检评分决定后续走快速通道（≥80→04b）还是完整流程（60-79→04→05），<60分退回03a重写

---

## 扩展技能

以下4个技能独立于主流水线，可在任意阶段单独调用：

| 技能 | 触发条件 | 用途 |
|------|---------|------|
| `小说拆文师` (06) | 用户说"拆文""分析对标作品""扒榜""扫榜""调研市场" | **V2.0升级**：前置扫榜调研（支持浏览器自动化/手动数据）→四维拆文（市场/角色/剧情/文风）→对标驱动模块化创作闭环→结构化输出到项目目录 |
| `小说导入师` (07) | 用户说"导入已有章节""已有正文需要续写""从正文重建工程" | **V2.0**：从已有正文反推设定/大纲/细纲/伏笔追踪，JSON Schema与00对齐，接入V5.2机制（INC/情绪锚点/02a接口），信号词置信度分级，自动生成首批衔接包 |
| `短篇写作技能` (08) | 用户说"写短篇""知乎盐言""故事会风格""8K-15K字""中篇15K-30K" | **V2.1**：三步完成短篇：定位→写作→去AI味（核心7Gate）。中篇15K-30K分段执行+迷你衔接包。15项自检，审美对齐04 V5.2 |
| `多视角审稿师` (09) | 用户说"审稿""帮我看看这篇""多视角评价" | 编辑/读者/AI鉴定/商业化四视角审稿，输出带优先级的修改建议 |

**调用方式**：直接在对话中告知用户这些技能的存在和使用场景，用户确认后调用对应 `Skill(name="xxx")`。

---

## 总控流程规则

### 一、启动阶段

1. 用户提供**核心灵感**和**基本要求**后，先不要直接调用子技能。
2. 先与用户确认以下信息（如用户已提供则跳过）：
   - 小说类型/题材（都市、玄幻、言情、悬疑等）
   - 目标平台（如番茄小说）和预期字数
   - 主角性别与基本定位
   - 有无特别偏好或禁忌（如不要后宫、不要虐主等）
3. **询问用户从哪一步开始**（见下方"指定起始步骤"规则）。
4. 确认完毕后，告知用户流程即将开始，进入对应步骤。

### 二、指定起始步骤

用户可以跳过前面已完成的步骤，直接从任意步骤开始。

**触发条件**：用户明确表示"从第X步开始"、"我已经有大纲了"、"直接写细纲"等。

**处理流程**：
1. 询问用户从哪一步开始（0-6步）。
2. 如果用户选择跳过前面的步骤，要求用户提供跳过步骤的产物（如已有大纲则提供大纲内容）。
3. 如果用户无法提供完整产物，询问是否基于已有内容补全缺失步骤，或强制跳过（可能影响后续质量）。
4. 确认起始步骤和所需材料后，直接从该步骤开始执行。

**示例**：
- 用户："已经有项目结构了，直接从设定开始。" → 确认项目目录 → 从第1步开始
- 用户："我已经有大纲了，直接从细纲开始。" → 要求提供大纲内容 → 从第3步开始
- 用户："设定和大纲都写好了，从第3步开始。" → 要求提供设定和大纲 → 从第3步开始
- 用户："直接从正文开始。" → 要求提供设定、大纲、细纲 → 从第4步开始
- 用户："正文已经写好了，只需要精修。" → 要求提供正文 → 从第5步开始

### 三、每阶段通用规则

**每个阶段（第1-6步）完成后，必须执行以下确认流程：**

1. 子技能输出产物后，向用户展示摘要。
2. 明确询问用户：
   - **"这个阶段的产物是否满意？不满意我可以重新生成或补充。"**
   - **"是否需要压缩上下文？（压缩后之前的详细内容会被精简，但核心设定会保留）"**
   - **"有没有需要补充的内容或修改意见？"**
3. 等待用户明确回复。用户说"可以"、"继续"、"下一步"等确认词后，才能进入下一阶段。
4. 如果用户要求修改，执行修改后重新确认，直到用户满意。

### 四、回退与返工

**本阶段不合格时，必须回退到依赖的上游阶段重新执行。**

| 当前步骤 | 如果产物不合格 | 回退到 |
|---------|-------------|--------|
| 第1步（设定） | 设定文档有根本性问题 | 重新与用户确认核心灵感 |
| 第2步（大纲） | 大纲方向不对/卖点不清晰 | 第1步（重新审查设定） |
| 第3步（细纲） | 细纲结构与大纲冲突 | 第2步（重新审查大纲） |
| 第4步（扩写） | 扩写偏离细纲/衔接包 | 第3步（重写该章细纲） |
| 第5步（精修） | 精修改动破坏了剧情逻辑 | 第4步（重新扩写后再精修） |
| 第6步（去AI味） | 去AI味后改变了文意的 | 退回04产出再处理 |

**原则**：下游发现问题 = 回溯到源头修复——不要在下游打补丁掩盖上游的错误。

### 四.5、正文偏差 → 逆向调整机制（V2.1.1 新增）

> 详见 [references/逆向调整机制.md](references/逆向调整机制.md)。
> **核心**：正文走向与上游产物产生偏差（且偏差方向更优）时，经用户确认后主动微调上游内容。六步法：识别→评估→用户确认→执行调整→一致性校验→恢复写作。
> **约束**：同卷≤2次/单次≤1设定字段+1关系线+1卷大纲/已精修章不可逆向/必须记录项目日志。

### 五、上下文压缩

- 当用户选择压缩上下文时，对已完成阶段的产物进行精简摘要，保留核心关键信息（人物名、关键设定、核心情节节点），删除冗余细节。
- 以精简格式输出压缩后的内容，作为后续阶段的上下文基础。
- 如果用户不压缩，则保留全部内容继续。
- **数值锚点不可压缩**（V2.8新增）：`number_anchors` 和 `power_system` 字段在任何压缩操作中必须完整保留，不得精简。这两个字段是全书数值和战力一致性的唯一基线，一旦在压缩中丢失，后续所有步骤的一致性校验将失效。

### 六、正文扩写特殊规则（第4步）

- 第4步（正文扩写）开始前，额外向用户确认：**"细纲已完成，是否直接开始进行小说正文扩写？"**
- 用户明确同意后才开始扩写。
- 正文扩写可选择"一次性全部生成"或"逐章生成，每章确认"，由用户决定。

### 六.5、三种写作流程（V5.1 新增，V5.2 更新）

> V5.1 将原来的单一流程扩展为三种，用户可在任何章节切换。默认使用 `/flow_full`。
> V5.2 更新：03b质检在三种流程中均为必执行步骤，不可跳过。
> **注意**：`/flow_*` 前缀控制流水线步骤数，与 AGENTS.md 中的 `/mode_*` 前缀（控制AI自主度）正交，可组合使用，如 `/mode_assisted /flow_fast`。

| 流程 | 前缀 | 流程 | 适用场景 | 上下文负担 |
|------|------|------|---------|-----------|
| **完整流程** | `/flow_full`（默认） | 03a→03b(必执行)→04(精修V5.2)→05(去AI味)→脚本收尾 | 重要章节、开篇、卷末、关键转折 | 高（4步全走） |
| **快速流程** | `/flow_fast` | 03a→03b(必执行)→04b(深度打磨)→脚本收尾 | 日常章节、过渡章节、已建立稳定节奏的连续章节 | 中（3步） |
| **生肉流程** | `/flow_raw` | 03a→03b(必执行)→05精简版→脚本收尾 | 初稿快速产出、后续批量精修 | 低（3步，但03b不可省） |

> **V5.2 重要变更**：三种流程中 03b（质检）均为**必执行**步骤，不可跳过。/flow_raw 虽然是最简流程，但03b质检仍必须执行——/flow_raw 的"简"体现在跳过04精修和05完整去AI味，而非跳过质检。

**快速通道准入条件（03b质检报告判断，4项全满足才走快速通道）：**

> 注：以下阈值的权威定义位于 scripts/lib/prose-utils.js，此处数值仅作参考。如需调整阈值，修改 prose-utils.js 中的常量定义即可，无需逐文件修改。

1. P0 5项必查全部通过
2. P1 8项抽查无S1级问题
3. 5维评分≥80分（**V5.2核心指标：03b质检评分≥80分自动进入快速通道**）
4. burstiness > BURSTINESS_CV_THRESHOLD（0.35）（句长方差检测）

> **V5.2 变更**：①quality-checker Agent从准入条件中移除（改为每章必执行，不再作为快速通道的额外门槛）；②"用户审美裁决"从必要条件改为可选步骤（用户可在03a后检查文风，但不阻塞流程）；③准入条件从5项精简为4项，核心判据为03b评分≥80分。
> Agent不可用时降级为主线程自检，不影响快速通道判定逻辑。

**流程切换规则：**
- 用户可随时用前缀切换流程：`/flow_fast 从下一章开始`
- 重要章节（开篇/卷末/高潮章）建议锁定 `/flow_full`，即使用户指定快速流程也应提醒
- `/flow_raw` 产出的章节必须在后续用 `/flow_full` 补全精修+去AI味

**用户审美检查点（V5.1 新增，V5.2 调整为可选）：**
- 03a扩写完成后、进入03b质检前，可向用户展示初稿前200字（可选步骤，不阻塞流程）
- 询问："这个文风和节奏是否符合你的预期？需要调整再继续质检。"
- 用户确认后进入03b；如需调整，修改后重新展示
- **V5.2 变更**：此检查点从"必经环节"降级为"可选环节"——用户可选择跳过直接进入03b质检。快速通道准入条件中不再将"用户审美裁决"作为必要条件

### 六.6、半自动批量模式（V5.4 新增）

> 详见 [references/批量模式.md](references/批量模式.md)。
> **核心**：`/batch N` 激活连续执行N章，仅blocking问题暂停（03b<60分/09审稿critical/脚本blocking/衔接包P0缺失/Delta降级连续2章）。
> **约束**：单次≤5章/03b不可跳过/连续2章评分下降>15分自动暂停/重要章节不受批量保护。
> **V6.0新增**：批量完成后自动运行 `check-rule-execution.js` 生成遥测报告，可选运行 `generate-dashboard.js` 生成HTML面板。

### 七、每3章一致性检查（第4步循环中触发 — V5.2更新）

**扩写完第3、6、9、12...章后（V5.2: 从每5章调整为每3章），暂停扩写，执行以下检查：**

1. **衔接包禁止矛盾链**：逐条核实最近3章的衔接包"禁止矛盾"条目，确认无一违反
2. **伏笔追踪表**：更新所有伏笔状态，确认ACTIVE伏笔有明确回收计划，DORMANT伏笔未触发时限
3. **角色状态漂移**：对照00设定文档，检查角色性格/能力是否与设定一致
4. **字数节奏**：最近3章的字数（4000±500）、对话比（≤60%）、场景数（3-5）是否符合契约
5. **数值与战力校验**（V2.8新增）：①扫描最近3章所有数值（年龄/距离/枚数/境界/物品数量/时间线），对照00设定的 `number_anchors` 字段，发现不一致即标记修正；②检查战斗场景中的角色表现是否与 `power_system` 矩阵的能力边界匹配，越级表现无设定依据即标记修正；③核实已标记为"已死亡"的角色是否以活人状态出现

**检查结果**：
- 全部通过 → 继续扩写
- 发现问题 → 标记问题章节，在扩写下一章时修正（不回溯已完成的章节，除非是P0级矛盾）

**中段加密检查**（V2.8新增，V5.2更新）：全书40%-60%区域（如全书50章则在第20-30章区间），将每3章检查改为每2章检查。实证研究表明AI长篇生成的错误集中在叙事中段，加密检查频率可更早捕获累积偏差。

### 八、精修与去AI味规则（第5-6步）

- 第5步精修：专注于潜台词打磨、金句提炼、个人风格注入。
- 第6步去AI味：去除AI写作痕迹，使文本更自然流畅。
- 两步独立执行，每步完成后都需要用户确认。

### 九、子技能调用方式

每个阶段调用对应的 Skill 工具：

```
第0步: Skill(name="项目初始化师")
第1步: Skill(name="小说设定架构师")
第2步: Skill(name="小说大纲构建师")
第3步: Skill(name="小说细纲编写技能")
第4a步: Skill(name="细纲扩写执行")          # V5.1: 03a_扩写执行.md
第4b步: Skill(name="质量自检")              # V5.1: 03b_质量自检.md
第5步: Skill(name="小说正文精修师")          # 或 Skill(name="深度打磨") 用于/flow_fast
第6步: Skill(name="去AI味精修师")
```

调用后，根据子技能展开的指令执行对应阶段的任务，完成后执行本技能的确认流程。

### 十、中断与恢复

- 用户可随时中断流程。下次继续时，告知用户当前处于哪个阶段，从该阶段继续。
- 如果用户想跳过某个阶段，必须明确确认，不可自行跳过。

### 十一、前置调研入口（v2.0 新增）

**在第2步（大纲）之前，建议先调用 06_小说拆文师 执行前置市场调研：**

1. 如果用户是新书项目（从第0步或第1步开始），在第2步前主动询问：**"是否需要先做前置市场调研（扫榜+拆文对标）？可以帮助确定题材方向和卖点。"**
2. 用户确认后，调用 `06_小说拆文师` 执行：扫榜（Phase 1-4）→ 拆文 → 对标数据写入 `对标/` 目录
3. 02 编写细纲时读取 `对标/` 数据，把对标书角色当功能位，反哺大纲设计

### 十二、滚动建纲触发（v2.0 新增，V5.2更新）

**第3步（细纲）默认采用滚动建纲模式：**

1. 首次产出前 10 章细纲 + 衔接包链（全书≤30章时可一次全建）
2. 进入第4步逐章扩写，每写完 3 章触发"补纲"：回到第3步补充后续 5-10 章细纲（V5.2: 从每5章调整为每3章）
3. 已进入正文写作的前 10 章细纲**锁定**，未经用户确认不得修改
4. 用户可指定"一次性全书建纲"替代滚动建纲

### 十三、确定性脚本收尾（v2.0 新增，V5.1扩展）

**第6步（去AI味）完成后，自动执行确定性脚本兜底检查：**

```
node scripts/check-ai-patterns.js --fail-on=blocking 正文文件
node scripts/check-degeneration.js --fail-on=blocking 正文文件
node scripts/normalize-punctuation.js --check 正文文件
node scripts/check-consistency.js --meta=设定/JSON元数据.json 正文文件
```

- `check-ai-patterns.js`：检测否定翻转/破折号/碎句号/长段落 → blocking 必须清零
- `check-degeneration.js`：检测逐字复读/截断/占位符/工程词泄漏 → blocking 必须清零
- `normalize-punctuation.js`：清理残留省略号/破折号 → --check 模式只报告
- `check-consistency.js`（V2.8新增）：检测年龄/距离/枚数/生死状态与JSON元数据的一致性 → blocking 必须清零
- 脚本只报告不修改，修改由 LLM 根据报告执行

**V5.1 新增辅助脚本（非收尾流程，按需调用）：**

```
# 跨章模式提取 — 03a扩写前运行，提取近N章句式/意象/身体反应/标点节奏
node scripts/extract-used-patterns.js --json --n=5 前5章文件...

# 衔接包压缩 — 衔接包链超10章时运行，旧包自动压缩为摘要
node scripts/compress-handoff.js --threshold=10 --keep=3 --write 衔接包文件...

# 版本同步检查 — 检查所有skill文件版本号与README是否一致
node scripts/sync-versions.js --write

# 情绪密度检测 — 03b质检阶段运行，检测情绪密度/多样性/单调/曲线
# --anchor参数传入02细纲中的"用户情绪锚点"字段值
node scripts/check-emotion-density.js --anchor=解气 正文文件
```

---

## 执行示例

**用户**: "我想写一本都市重生小说，主角回到2000年，用互联网知识改变命运。"

**AI**: 
1. 确认补充信息（目标平台、字数、偏好等）
2. 确认完毕后进入第0步：调用 `项目初始化师`，创建标准目录结构
3. 进入第1步：调用 `小说设定架构师`，生成设定文档
4. 完成后展示摘要，询问确认 → 压缩 → 补充
5. **询问是否前置调研** → 用户确认 → 调用 `小说拆文师` 扫榜+拆文对标
6. 进入第2步（大纲），读取对标数据辅助设计
7. 进入第3步（细纲），**滚动建纲**先出前10章
8. 第3步完成后进入第4步（扩写），**每写3章触发补纲**
9. 第4步中每3章暂停，执行一致性检查 + 09多视角审稿
10. 第5-6步分别精修和去AI味
11. 第6步后执行**确定性脚本收尾**（3个脚本）
12. 最终输出完整小说

---

## 注意事项

1. **不可自行跨越步骤**：每步必须等待用户确认后才能进入下一步。
2. **指定起始步骤时必须验证**：如果用户跳过前面步骤，必须要求提供已有产物，否则质量无法保证。
3. **上下文压缩是可选的**：用户可以选择压缩或不压缩，尊重用户选择。
4. **精修和去AI味是独立步骤**：不要将第5步和第6步合并执行。
5. **第0步为全书奠基步骤**：即使从后续步骤开始，也应确认项目目录结构是否存在。
6. **回退不是惩罚**：下游发现问题回溯到源头修改是正确做法，不要为了"往前走"而掩盖错误。
7. **扩展技能独立可用**：拆文/导入/审稿/短篇可在任何阶段独立调用，不依赖主流水线进度。
8. **前置调研建议但不强制**：新书项目在第2步前建议做前置调研，但用户可跳过。
9. **滚动建纲为默认**：第3步默认滚动建纲，用户可指定一次性全书建纲。
10. **三种写作流程（V5.1新增，V5.2更新）**：`/flow_full`（完整4步）、`/flow_fast`（快速3步）、`/flow_raw`（生肉3步，03b不可省）。重要章节建议锁定完整流程。与 `/mode_*` 行为模式正交可组合。
11. **03a/03b不可跳过**（V5.2强化）：即使使用快速模式或生肉模式，03a扩写和03b质检都必须执行。03b必须在独立上下文中运行（禁止在03a同一会话中继续），质检评分≥80分自动走快速通道（04b），60-79分走完整流程（04→05），<60分退回03a重写。
12. **跨章模式提取（V5.1新增）**：03a扩写前建议运行 `extract-used-patterns.js` 提取近5章句式/意象模式，避免跨章重复。
13. **对抗性质检Agent（V5.1新增，V5.2强化）**：03b质检阶段**每章必调用** `quality-checker` Agent（.claude/agents/）进行独立找茬审查，Agent不可用时降级为主线程自检。V5.2: 从"关键章节时启用"改为"每章必执行"。
14. **确定性脚本是兜底**：脚本只报告不修改，blocking 级 finding 必须清零后才能定稿。
15. **跨平台部署**：skill 文件平台无关，hooks/settings 按当前平台约定部署。
16. **每3章审稿触发**（V5.2新增）：09多视角审稿从"每5章"调整为"每3章"（第3/6/9/12...章后触发），与一致性检查同步执行。

---

## AI使用策略指导（v2.3 新增 — 明确AI能力的边界）

> 详见 [references/AI使用策略.md](references/AI使用策略.md)。
> **核心理念**：AI是"打字员+校对员+结构工程师"，不是"作家"。让AI做AI擅长的事（结构/一致性/量产/检测），让人做人擅长的事（情感判断/创意跳跃/读者共鸣/审美决策）。
> 用户在流水线中的四个角色：创意总监（00-02）→ 审美裁决者（04）→ 情感校准器（03后）→ 质量守门人（05后）。

---

## 规则分级与执行遥测（V6.0 新增）

> **规则分级**：`samples/config/rules.json` V6.0 将所有规则分为三档：
> - **hard（硬规则）**：事实性/一致性/结构性规则，脚本强制校验，违反=阻断输出。如 R004情绪锚点/R008衔接包压缩/R011角色一致性/R015用户情绪锚点/R016冲突四件套/R017锐度保护/R021混合评分/R025执行遥测。
> - **soft（软规则）**：风格性/节奏性规则，脚本报告但不阻断，LLM可自行判断。如 R001否定排比/R005解释腔/R006套话/R007跨章去重/R012节奏波形/R013退化检测/R019AI过渡句/R022文风一致性/R024读者留存。
> - **adaptive（自适应规则）**：密度性/比例性规则，根据章节类型（核心/重要/过场/点缀）自动调整阈值。如 R003比喻质量/R009 burstiness/R010情绪词密度/R014标点限额/R018 TTR/R020信息密度/R023章中钩子。

> **执行遥测**：运行 `node scripts/check-rule-execution.js --project <项目路径>` 统计每章规则执行率，输出到 `追踪/rule-execution-telemetry.json`。运行 `node scripts/generate-dashboard.js --project <项目路径>` 生成HTML进度面板。

> **规则版本化**：每条规则有独立 `rule_version` 和 `changelog`。在 `novel_meta.json` 中设置 `rule_lock: true` 可锁定创建时的规则版本，不受后续更新影响。`rule_lock: false`（默认）跟随最新规则。
